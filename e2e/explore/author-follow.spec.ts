import { test, expect } from "@playwright/test";

test.describe("Explore - Author Follow", () => {
  // test("should navigate to book details from explore", async ({ page }) => {
  //   await page.goto("/explore");
  //   const bookLink = page.locator('a[href^="/book/"]').first();
  //   await bookLink.waitFor({ state: "visible", timeout: 10000 });
  //   await bookLink.click();
  //   await expect(page).toHaveURL(/\/explore\//);
  // });
  // test('should show Autores tab on my-session page', async ({ page }) => {
  //   await page.goto('/my-session')
  //   await page.waitForSelector('button:has-text("Autores")', { timeout: 10000 })
  //   await expect(page.locator('button:has-text("Autores")')).toBeVisible()
  // })
});
